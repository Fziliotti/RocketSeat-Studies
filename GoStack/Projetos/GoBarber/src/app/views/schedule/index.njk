{% extends "_layouts/default.njk" %}

{% block body %}
  <div class="content">
    <strong>Seus agendamentos</strong>
    {# <p>{{req.session.user}}, a lista de agendamentos que você tem para hoje:</p> #}

    <ul class="providers">
      {% for appointment in appointments %}
        <li class="provider">
          <div>
          ae
            {# <img src="/files/{{ appointment.user.avatar }}" /> #}
            {# Uso de filtro para transformar em uma data legivel #}
            {# <strong>{{ appointment.user.name }} <br> {{ appointment.date | date("HH:mm") }}</strong> #}
          </div>
        </li>
      {% endfor %}
    </ul>

    <a href="/app/dashboard" class="logout">Voltar</a>
  </div>
{% endblock %}
